(* CS421 - Fall 2017
 * closer_to_origin
 *
 * Please keep in mind that there may be more than one
 * way to solve a problem.  You may want to change how this starts.
 *)


(*Problem*)
let swap (a, b) = (b, a);;
let swap_eq p1 p2 = p1 = swap(p2);;
